// const mobileController = require('../controllers/mobile');

// function promise(index) {
//   return new Promise((resolve) => {
//     const delay = Math.random() * 50000; // between 0 and 5 seconds
//     console.log(`${index}. Waiting ${delay}`);
//     setTimeout(() => {
//       mobileController.test();
//       console.log(`${index}. Done waiting ${delay}`);
//       resolve();
//     }, delay);
//   });
// }
// console.log('hello', Promise.all([
//   promise(1),
//   promise(2),
//   promise(3),
//   promise(4),
//   promise(5)
// ])
//   .then(() => console.log('All done!')));

